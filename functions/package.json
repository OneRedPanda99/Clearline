{
  "name": "clearline-functions",
  "description": "Clearline Cloud Functions — receipt OCR, Twilio SMS, Hermes drafts",
  "engines": {
    "node": "20 || 22"
  },
  "main": "index.js",
  "scripts": {
    "lint": "node --check index.js && node --check lib/receipts.js && node --check lib/sms.js && node --check lib/hermes.js",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1",
    "twilio": "^5.3.5"
  },
  "private": true
}
